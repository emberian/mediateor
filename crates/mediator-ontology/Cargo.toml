[package]
name = "mediator-ontology"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
mediator-types = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
# the string-diagram substrate (pushout = coequalizer of coproduct). Published
# crate (not a local path) so the workspace is portable to the deploy box.
open-hypergraphs = "0.3"
