[package]
name = "mediator-ontology"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
mediator-types = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
# the string-diagram substrate (pushout = coequalizer of coproduct); optional —
# fall back to a local structural representation if its API is awkward.
open-hypergraphs = { path = "/Users/ember/hellas/open-hypergraphs" }
